# Universal Protocol Bridge
This repository contains a complete USB adapter solution consisting of:

- **Adapter firmware** – **STM32-based** firmware implementing a **USB CDC** device and peripheral bridging

- **Desktop application** – a host-side application used to communicate with the adapter over **USB**

Each project directory contains its own ```README.md``` with detailed build and usage instructions